machine:
  node:
    version: 6
dependencies:
  pre:
    - npm install -g yarn codecov
  override:
    - yarn
test:
  override:
    - npm test -- --coverage
  post:
    - codecov
general:
  artifacts:
    - coverage
